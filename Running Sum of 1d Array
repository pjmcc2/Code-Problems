class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        running_sum = 0
        output_array = []
        for i in range(len(nums)):
            running_sum += nums[i]
            output_array.append(running_sum)
        return output_array
